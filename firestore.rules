rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tournament public read for live screens/boards, write for operators
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

